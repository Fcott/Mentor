<div class="nested-fields">
  <%= f.collection_select(:job_category_id, JobCategory.all, :id, :name, prompt: true) %>
  <%= f.hidden_field :user_id, value: @user.id %>
  <%= link_to_remove_association "Remove", f, wrapper_class: "nested-fields" %>
</div>
